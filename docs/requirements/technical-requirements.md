# Technical Requirements

## Environment

- OS:
- Gazebo version:
- Shell:
- Python version:

## Runtime Requirements

- Gazebo CLI must be available as `gz`.
- The project must run with `gz sim worlds/basic_terrain.sdf`.
- Model lookup must use `GZ_SIM_RESOURCE_PATH`.

## Development Requirements

- Static tests use `pytest`.

## Constraints

- This is a standalone Gazebo project.
- Do not use ROS, ROS 2, `ros2 launch`, colcon, or ROS packages.
